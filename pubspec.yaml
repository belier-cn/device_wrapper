name: device_wrapper
description: A Flutter package to wrap pages with realistic device frames (iPhone 17 Pro, iPad Gen 11) for web/desktop preview. Features auto-scaling, Dynamic Island, and smooth mode switching.
version: 1.0.0
homepage: https://github.com/citiasia-inc/device_wrapper
repository: https://github.com/citiasia-inc/device_wrapper
issue_tracker: https://github.com/citiasia-inc/device_wrapper/issues

topics:
  - ui
  - device-preview
  - responsive
  - web
  - simulator

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
